# Dependencies & build output
node_modules/
dist/

# Cloudflare Wrangler local state (local D1, cache, dev artifacts)
.wrangler/

# Secrets / local environment
.dev.vars
.env
*.local

# Claude Code / ruflo tooling (machine-specific, regenerated by the tooling)
.claude/
.claude-flow/

# OS cruft
.DS_Store
Thumbs.db

# Kindle kiosk kill-switch (device-only artifact)
extensions/kindletodo/DISABLE

# Device error-screen assets (downloaded at deploy time)
extensions/kindletodo/assets/

# Local AI-tooling config (ruflo / Claude Code) — not part of the project
CLAUDE.md
.mcp.json
