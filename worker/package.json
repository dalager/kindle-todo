{
  "name": "kindletodo-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply kindletodo --local",
    "migrate:remote": "wrangler d1 migrations apply kindletodo --remote"
  },
  "dependencies": {
    "@cf-wasm/og": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "@types/react": "^18.3.31",
    "react": "^18.3.1",
    "wrangler": "^4"
  }
}
